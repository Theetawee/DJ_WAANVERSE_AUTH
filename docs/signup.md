required params
1. identifier (email or phone based on config)
2. password

optional 
turnstile_token - if turnstile is enabled


settings include `DISABLE_SIGNUP`
    `AUTHENTICATION_IDENTIFIERS`
    `TURNSTILE_ENABLED`
   `TURNSTILE_SECRET_KEY`