Packages ships with the following email templates.

1. emails/login_code.html
    1. context :code (the code to be entered)
    2. user : account (the account model)
    3. user_name : user_name (Displayed as full name)
